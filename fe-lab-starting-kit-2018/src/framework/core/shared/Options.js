/** Export Options to represent notifications. */
export default class Options {
    constructor(type,text,showTime) {
        this.type = type;
        this.text = text;
        this.showTime = showTime;
    }
}